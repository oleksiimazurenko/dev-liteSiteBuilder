import { RegisterForm } from "@/features/main/auth";

export default function Register() {
  return (
    <div className="flex min-h-[calc(100svh-59.5px)] items-center justify-center">
      <RegisterForm />
    </div>
  );
}
