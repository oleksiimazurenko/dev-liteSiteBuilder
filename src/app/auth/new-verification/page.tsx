import { NewVerificationForm } from "@/features/main/auth";

const NewVerificationPage = () => {
  return (
    <div className="flex min-h-[calc(100svh-59.5px)] items-center justify-center">
      <NewVerificationForm />;
    </div>
  );
};

export default NewVerificationPage;
