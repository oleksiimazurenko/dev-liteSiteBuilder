import { ResetForm } from "@/features/main/auth";

export default function Reset() {
  return (
    <div className="flex min-h-[100svh] items-center justify-center">
      <ResetForm />
    </div>
  );
}
